---
import BaseLayout from '../layouts/BaseLayout.astro';
import Hero from '../components/Hero.astro';
import Section from '../components/Section.astro';
import ProjectCard from '../components/ProjectCard.astro';
import BlogCard from '../components/BlogCard.astro';
import { getCollection } from 'astro:content';

// Fetch GitHub repos at build time, sorted by stars
async function getGitHubRepos() {
  try {
    const response = await fetch('https://api.github.com/users/cainky/repos?sort=stars&direction=desc&per_page=30');
    if (!response.ok) throw new Error('Failed to fetch repos');
    const repos = await response.json();
    return repos
      .filter((repo: any) => !repo.fork && !repo.archived)
      .sort((a: any, b: any) => b.stargazers_count - a.stargazers_count)
      .slice(0, 5)
      .map((repo: any) => ({
        name: repo.name,
        description: repo.description,
        url: repo.html_url,
        language: repo.language,
        stars: repo.stargazers_count,
        topics: repo.topics || [],
      }));
  } catch (error) {
    console.error('Error fetching GitHub repos:', error);
    return [];
  }
}

// Get blog posts
const allPosts = await getCollection('blog', ({ data }) => !data.draft);
const recentPosts = allPosts
  .sort((a, b) => b.data.date.valueOf() - a.data.date.valueOf())
  .slice(0, 3);

const projects = await getGitHubRepos();
---

<BaseLayout title="Kyle Cain | Software Developer">
  <Hero />

  <!-- Tech Stack Section -->
  <Section class="relative">
    <div class="text-center mb-8 sm:mb-12 animate-on-scroll">
      <h2 class="text-2xl md:text-3xl font-bold mb-3">
        Technologies I <span class="text-gradient">work with</span>
      </h2>
      <p class="text-text-secondary text-sm sm:text-base max-w-2xl mx-auto">
        Building modern applications with cutting-edge tools
      </p>
    </div>
    <div class="flex flex-wrap justify-center gap-3 sm:gap-4 animate-on-scroll" style="animation-delay: 0.1s">
      {[
        { name: 'TypeScript', color: '#3178c6' },
        { name: 'Python', color: '#3776ab' },
        { name: 'React', color: '#61dafb' },
        { name: 'Node.js', color: '#339933' },
        { name: 'PostgreSQL', color: '#336791' },
        { name: 'Docker', color: '#2496ed' },
        { name: 'Cloud Infra', color: '#ff9900' },
        { name: 'Git', color: '#f05032' },
      ].map((tech) => (
        <div
          class="group px-4 py-2 sm:px-5 sm:py-2.5 bg-surface border border-border rounded-full hover:border-primary/50 transition-all duration-300 hover:scale-105 cursor-default"
        >
          <span class="flex items-center gap-2">
            <span
              class="w-2 h-2 rounded-full"
              style={`background-color: ${tech.color}`}
            ></span>
            <span class="text-sm sm:text-base text-text-secondary group-hover:text-text-primary transition-colors">
              {tech.name}
            </span>
          </span>
        </div>
      ))}
    </div>
  </Section>

  <!-- Featured Projects Section -->
  <Section class="relative">
    <div class="flex flex-col sm:flex-row sm:items-end sm:justify-between gap-4 mb-8 sm:mb-12">
      <div class="animate-on-scroll">
        <h2 class="text-2xl md:text-3xl font-bold mb-2">
          Featured <span class="text-gradient">Projects</span>
        </h2>
        <p class="text-text-secondary text-sm sm:text-base">
          Open source projects sorted by popularity
        </p>
      </div>
      <a href="/projects" class="btn-secondary animate-on-scroll w-fit" style="animation-delay: 0.1s">
        View All Projects
      </a>
    </div>
    {projects.length > 0 ? (
      <div class="grid gap-4">
        {projects.map((project: any, index: number) => (
          <div class="animate-on-scroll" style={`animation-delay: ${index * 0.08}s`}>
            <ProjectCard {...project} />
          </div>
        ))}
      </div>
    ) : (
      <p class="text-text-secondary">Loading projects...</p>
    )}
  </Section>

  <!-- Recent Blog Posts Section -->
  <Section class="relative">
    <div class="flex flex-col sm:flex-row sm:items-end sm:justify-between gap-4 mb-8 sm:mb-12">
      <div class="animate-on-scroll">
        <h2 class="text-2xl md:text-3xl font-bold mb-2">
          Recent <span class="text-gradient">Articles</span>
        </h2>
        <p class="text-text-secondary text-sm sm:text-base">
          Thoughts on software development and best practices
        </p>
      </div>
      <a href="/blog" class="btn-secondary animate-on-scroll w-fit" style="animation-delay: 0.1s">
        Read All Articles
      </a>
    </div>
    {recentPosts.length > 0 ? (
      <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-4 sm:gap-6">
        {recentPosts.map((post, index) => (
          <div class="animate-on-scroll" style={`animation-delay: ${index * 0.1}s`}>
            <BlogCard
              title={post.data.title}
              description={post.data.description}
              slug={post.id.replace(/\.md$/, '')}
              date={post.data.date}
              image={post.data.image}
              category={post.data.category}
            />
          </div>
        ))}
      </div>
    ) : (
      <p class="text-text-secondary">No posts yet.</p>
    )}
  </Section>

  <!-- CTA Section -->
  <Section class="relative">
    <div class="animate-on-scroll text-center px-2">
      <div class="inline-block mb-6">
        <span class="px-4 py-1.5 text-xs sm:text-sm font-medium bg-primary/10 text-primary rounded-full border border-primary/20">
          Open to opportunities
        </span>
      </div>
      <h2 class="text-2xl sm:text-3xl md:text-4xl font-bold mb-4">
        Let's build something <span class="text-gradient">amazing</span> together
      </h2>
      <p class="text-text-secondary text-sm sm:text-base max-w-xl mx-auto mb-8">
        I'm always excited to collaborate on interesting projects and explore new opportunities.
        Whether you have a question or just want to say hi, feel free to reach out!
      </p>
      <div class="flex flex-col sm:flex-row gap-3 sm:gap-4 justify-center">
        <a
          href="https://www.linkedin.com/in/kyle-cain/"
          target="_blank"
          rel="noopener noreferrer"
          class="btn-primary group"
        >
          <span class="flex items-center justify-center gap-2">
            <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
              <path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433c-1.144 0-2.063-.926-2.063-2.065 0-1.138.92-2.063 2.063-2.063 1.14 0 2.064.925 2.064 2.063 0 1.139-.925 2.065-2.064 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/>
            </svg>
            Connect on LinkedIn
          </span>
        </a>
        <a
          href="https://github.com/cainky"
          target="_blank"
          rel="noopener noreferrer"
          class="btn-secondary group"
        >
          <span class="flex items-center justify-center gap-2">
            <svg class="w-5 h-5" fill="currentColor" viewBox="0 0 24 24">
              <path fill-rule="evenodd" d="M12 2C6.477 2 2 6.484 2 12.017c0 4.425 2.865 8.18 6.839 9.504.5.092.682-.217.682-.483 0-.237-.008-.868-.013-1.703-2.782.605-3.369-1.343-3.369-1.343-.454-1.158-1.11-1.466-1.11-1.466-.908-.62.069-.608.069-.608 1.003.07 1.531 1.032 1.531 1.032.892 1.53 2.341 1.088 2.91.832.092-.647.35-1.088.636-1.338-2.22-.253-4.555-1.113-4.555-4.951 0-1.093.39-1.988 1.029-2.688-.103-.253-.446-1.272.098-2.65 0 0 .84-.27 2.75 1.026A9.564 9.564 0 0112 6.844c.85.004 1.705.115 2.504.337 1.909-1.296 2.747-1.027 2.747-1.027.546 1.379.202 2.398.1 2.651.64.7 1.028 1.595 1.028 2.688 0 3.848-2.339 4.695-4.566 4.943.359.309.678.92.678 1.855 0 1.338-.012 2.419-.012 2.747 0 .268.18.58.688.482A10.019 10.019 0 0022 12.017C22 6.484 17.522 2 12 2z" clip-rule="evenodd" />
            </svg>
            Follow on GitHub
          </span>
        </a>
      </div>
    </div>
  </Section>
</BaseLayout>
